diff --git a/src/include/postgres_utils.hpp b/src/include/postgres_utils.hpp
index 34d7368..4acdfdb 100644
--- a/src/include/postgres_utils.hpp
+++ b/src/include/postgres_utils.hpp
@@ -14,6 +14,7 @@
 #include "duckdb/main/secret/secret_manager.hpp"
 
 namespace duckdb {
+class PostgresConnection;
 class PostgresSchemaEntry;
 class PostgresTransaction;
 
@@ -21,6 +22,7 @@ struct PostgresTypeData {
 	int64_t type_modifier = 0;
 	string type_name;
 	string type_schema;
+	int64_t type_oid = 0;
 	idx_t array_dimensions = 0;
 };
 
@@ -69,7 +71,7 @@ public:
 	static LogicalType ToPostgresType(const LogicalType &input);
 	static LogicalType TypeToLogicalType(optional_ptr<PostgresTransaction> transaction,
 	                                     optional_ptr<PostgresSchemaEntry> schema, const PostgresTypeData &input,
-	                                     PostgresType &postgres_type);
+	                                     PostgresType &postgres_type, PostgresConnection *connection = nullptr);
 	static string TypeToString(const LogicalType &input);
 	static string PostgresOidToName(uint32_t oid);
 	static uint32_t ToPostgresOid(const LogicalType &input);
diff --git a/src/include/storage/postgres_table_set.hpp b/src/include/storage/postgres_table_set.hpp
index 68877c6..2b4157b 100644
--- a/src/include/storage/postgres_table_set.hpp
+++ b/src/include/storage/postgres_table_set.hpp
@@ -50,11 +50,12 @@ protected:
 	void AlterTable(ClientContext &context, PostgresTransaction &transaction, RemoveColumnInfo &info);
 
 	static void AddColumn(optional_ptr<PostgresTransaction> transaction, optional_ptr<PostgresSchemaEntry> schema,
-	                      PostgresResult &result, idx_t row, PostgresTableInfo &table_info);
+	                      PostgresResult &result, idx_t row, PostgresTableInfo &table_info,
+	                      PostgresConnection *connection = nullptr);
 	static void AddConstraint(PostgresResult &result, idx_t row, PostgresTableInfo &table_info);
 	static void AddColumnOrConstraint(optional_ptr<PostgresTransaction> transaction,
 	                                  optional_ptr<PostgresSchemaEntry> schema, PostgresResult &result, idx_t row,
-	                                  PostgresTableInfo &table_info);
+	                                  PostgresTableInfo &table_info, PostgresConnection *connection = nullptr);
 
 	void CreateEntries(PostgresTransaction &transaction, PostgresResult &result, idx_t start, idx_t end);
 
diff --git a/src/postgres_utils.cpp b/src/postgres_utils.cpp
index 9edeff3..15ef34a 100644
--- a/src/postgres_utils.cpp
+++ b/src/postgres_utils.cpp
@@ -1,4 +1,5 @@
 #include "postgres_utils.hpp"
+#include "postgres_connection.hpp"
 #include "storage/postgres_schema_entry.hpp"
 #include "storage/postgres_transaction.hpp"
 #include "postgres_type_oids.hpp"
@@ -179,7 +180,8 @@ uint32_t PostgresUtils::TypeNameToPostgresOid(const string &type_name) {
 
 LogicalType PostgresUtils::TypeToLogicalType(optional_ptr<PostgresTransaction> transaction,
                                              optional_ptr<PostgresSchemaEntry> schema,
-                                             const PostgresTypeData &type_info, PostgresType &postgres_type) {
+                                             const PostgresTypeData &type_info, PostgresType &postgres_type,
+                                             PostgresConnection *connection) {
 	auto &pgtypename = type_info.type_name;
 
 	// postgres array types start with an _
@@ -207,8 +209,16 @@ LogicalType PostgresUtils::TypeToLogicalType(optional_ptr<PostgresTransaction> t
 		child_type_info.type_name = pgtypename.substr(1);
 		child_type_info.type_modifier = type_info.type_modifier;
 		child_type_info.type_schema = type_info.type_schema;
+		if (connection && type_info.type_oid != 0) {
+			auto elem_result = connection->Query(
+			    nullptr, StringUtil::Format("SELECT typelem FROM pg_type WHERE oid = %d", type_info.type_oid));
+			if (elem_result->Count() > 0) {
+				child_type_info.type_oid = elem_result->GetInt64(0, 0);
+			}
+		}
 		PostgresType child_pg_type;
-		auto child_type = PostgresUtils::TypeToLogicalType(transaction, schema, child_type_info, child_pg_type);
+		auto child_type =
+		    PostgresUtils::TypeToLogicalType(transaction, schema, child_type_info, child_pg_type, connection);
 		// populate the child OID from the actual Postgres type name
 		if (child_pg_type.oid == 0) {
 			child_pg_type.oid = TypeNameToPostgresOid(child_type_info.type_name);
@@ -301,6 +311,48 @@ LogicalType PostgresUtils::TypeToLogicalType(optional_ptr<PostgresTransaction> t
 	}
 
 	if (!transaction || !schema || type_info.type_schema.empty()) {
+		if (connection && type_info.type_oid != 0) {
+			// no catalog to resolve the type in, but we can ask Postgres directly
+			auto query = StringUtil::Format("SELECT a.attname, t.typname, a.atttypmod, a.atttypid "
+			                                "FROM pg_type ct "
+			                                "JOIN pg_class c ON c.oid = ct.typrelid "
+			                                "JOIN pg_attribute a ON a.attrelid = ct.typrelid "
+			                                "JOIN pg_type t ON t.oid = a.atttypid "
+			                                "WHERE ct.oid = %d AND ct.typtype = 'c' "
+			                                "AND a.attnum > 0 AND NOT a.attisdropped "
+			                                "ORDER BY a.attnum",
+			                                type_info.type_oid);
+			auto result = connection->Query(nullptr, query);
+			auto rows = result->Count();
+			if (rows > 0) {
+				child_list_t<LogicalType> child_types;
+				for (idx_t row = 0; row < rows; row++) {
+					auto field_name = result->GetString(row, 0);
+					PostgresTypeData field_type_info;
+					field_type_info.type_name = result->GetString(row, 1);
+					field_type_info.type_modifier = result->GetInt64(row, 2);
+					field_type_info.type_oid = result->GetInt64(row, 3);
+					PostgresType child_pg_type;
+					auto field_type = TypeToLogicalType(nullptr, nullptr, field_type_info, child_pg_type, connection);
+					child_types.push_back(make_pair(field_name, field_type));
+					postgres_type.children.push_back(std::move(child_pg_type));
+				}
+				return LogicalType::STRUCT(std::move(child_types));
+			}
+			auto domain_query = StringUtil::Format("SELECT bt.typname, t.typtypmod, bt.oid "
+			                                       "FROM pg_type t "
+			                                       "JOIN pg_type bt ON bt.oid = t.typbasetype "
+			                                       "WHERE t.oid = %d AND t.typtype = 'd'",
+			                                       type_info.type_oid);
+			auto domain_result = connection->Query(nullptr, domain_query);
+			if (domain_result->Count() > 0) {
+				PostgresTypeData base_type_info;
+				base_type_info.type_name = domain_result->GetString(0, 0);
+				base_type_info.type_modifier = domain_result->GetInt64(0, 1);
+				base_type_info.type_oid = domain_result->GetInt64(0, 2);
+				return TypeToLogicalType(nullptr, nullptr, base_type_info, postgres_type, connection);
+			}
+		}
 		// unsupported, or no type schema to resolve custom types in
 		// (information_schema introspection) - fallback to varchar
 		postgres_type.info = PostgresTypeAnnotation::CAST_TO_VARCHAR;
diff --git a/src/storage/postgres_table_set.cpp b/src/storage/postgres_table_set.cpp
index d5142bc..9f5e7fc 100644
--- a/src/storage/postgres_table_set.cpp
+++ b/src/storage/postgres_table_set.cpp
@@ -32,7 +32,7 @@ SELECT pg_namespace.oid AS namespace_id, relname, relpages, attname,
     attnum, pg_attribute.attnotnull AS notnull, NULL constraint_id,
     NULL constraint_type, NULL constraint_key, type_ns.nspname AS type_schema,
     col_desc.description AS column_comment,
-    tbl_desc.description AS table_comment
+    tbl_desc.description AS table_comment, atttypid type_oid
 FROM pg_class
 JOIN pg_namespace ON relnamespace = pg_namespace.oid
 JOIN pg_attribute ON pg_class.oid=pg_attribute.attrelid
@@ -46,7 +46,7 @@ SELECT pg_namespace.oid AS namespace_id, relname, NULL relpages, NULL attname, N
     NULL type_modifier, NULL ndim, NULL attnum, NULL AS notnull,
     pg_constraint.oid AS constraint_id, contype AS constraint_type,
     conkey AS constraint_key, NULL AS type_schema,
-    NULL AS column_comment, NULL AS table_comment
+    NULL AS column_comment, NULL AS table_comment, NULL AS type_oid
 FROM pg_class
 JOIN pg_namespace ON relnamespace = pg_namespace.oid
 JOIN pg_constraint ON (pg_class.oid=pg_constraint.conrelid)
@@ -75,7 +75,7 @@ SELECT table_schema AS namespace_id, table_name AS relname, 0 AS relpages, colum
     data_type AS type_name, -1 AS type_modifier, 0 AS ndim, ordinal_position AS attnum,
     CASE WHEN is_nullable = 'NO' THEN 't' ELSE 'f' END AS notnull,
     NULL AS constraint_id, NULL AS constraint_type, NULL AS constraint_key,
-    NULL AS type_schema, NULL AS column_comment, NULL AS table_comment
+    NULL AS type_schema, NULL AS column_comment, NULL AS table_comment, NULL AS type_oid
 FROM information_schema.columns
 WHERE table_schema NOT IN ('information_schema', 'pg_catalog', 'pg_toast') ${CONDITION}
 ORDER BY table_schema, table_name, ordinal_position;
@@ -92,7 +92,7 @@ ORDER BY table_schema, table_name, ordinal_position;
 
 void PostgresTableSet::AddColumn(optional_ptr<PostgresTransaction> transaction,
                                  optional_ptr<PostgresSchemaEntry> schema, PostgresResult &result, idx_t row,
-                                 PostgresTableInfo &table_info) {
+                                 PostgresTableInfo &table_info, PostgresConnection *connection) {
 	PostgresTypeData type_info;
 	idx_t column_index = 3;
 	auto column_name = result.GetString(row, column_index);
@@ -105,10 +105,14 @@ void PostgresTableSet::AddColumn(optional_ptr<PostgresTransaction> transaction,
 	if (!result.IsNull(row, type_schema_index)) {
 		type_info.type_schema = result.GetString(row, type_schema_index);
 	}
+	idx_t type_oid_index = column_index + 12;
+	if (!result.IsNull(row, type_oid_index)) {
+		type_info.type_oid = result.GetInt64(row, type_oid_index);
+	}
 	string default_value;
 
 	PostgresType postgres_type;
-	auto column_type = PostgresUtils::TypeToLogicalType(transaction, schema, type_info, postgres_type);
+	auto column_type = PostgresUtils::TypeToLogicalType(transaction, schema, type_info, postgres_type, connection);
 	table_info.postgres_types.push_back(std::move(postgres_type));
 	table_info.postgres_names.push_back(column_name);
 	ColumnDefinition column(std::move(column_name), std::move(column_type));
@@ -156,12 +160,13 @@ void PostgresTableSet::AddConstraint(PostgresResult &result, idx_t row, Postgres
 
 void PostgresTableSet::AddColumnOrConstraint(optional_ptr<PostgresTransaction> transaction,
                                              optional_ptr<PostgresSchemaEntry> schema, PostgresResult &result,
-                                             idx_t row, PostgresTableInfo &table_info) {
+                                             idx_t row, PostgresTableInfo &table_info,
+                                             PostgresConnection *connection) {
 	if (result.IsNull(row, 3)) {
 		// constraint
 		AddConstraint(result, row, table_info);
 	} else {
-		AddColumn(transaction, schema, result, row, table_info);
+		AddColumn(transaction, schema, result, row, table_info, connection);
 	}
 }
 
@@ -264,7 +269,7 @@ unique_ptr<PostgresTableInfo> PostgresTableSet::GetTableInfo(ClientContext &cont
 	}
 	auto table_info = make_uniq<PostgresTableInfo>(schema_name, table_name);
 	for (idx_t row = 0; row < rows; row++) {
-		AddColumnOrConstraint(nullptr, nullptr, *result, row, *table_info);
+		AddColumnOrConstraint(nullptr, nullptr, *result, row, *table_info, &connection);
 	}
 	table_info->approx_num_pages = result->IsNull(0, 2) ? 0 : result->GetInt64(0, 2);
 	// Read table-level comment
